- clean: [
    '~',
]

- link:
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.dircolors: .dircolors
    ~/.config/rofi/config:
        create: true
        path: rofi.config
    ~/.zshrc: .zshrc

- shell:
    - ["git submodule update --init --recursive", Installing submodules]
